# Fitment data

Public `fitments` stays empty until gated. Shop Floor UI screens use Example placeholders only — never invent Confirmed.

Hub model: independent `pcd` and `centreBore` fields; `hubUnknownSummary` is aggregate only.
